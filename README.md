# WatchOS2 realm sample
- 以下のビルドエラーが出ます

```
ld: in /Users/mogmet/Work/iOS/sample/RealmSample/Pods/Realm/core/librealm-watchos.a(librealm-WatchSimulator-prelink.a.o), building for watchOS simulator, but linking in object file built for (unknown), for architecture i386
clang: error: linker command failed with exit code 1 (use -v to see invocation)
```
